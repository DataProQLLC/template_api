# to generate: uv pip compile requirements.in -o requirements.txt --generate-hashes

# to upgrade: uv pip compile requirements.in -o requirements.txt --generate-hashes --upgrade
# git diff requirements.txt

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic-settings>=2.5
pydantic[email]>=2.9
pyjwt[crypto]>=2.9
httpx>=0.27